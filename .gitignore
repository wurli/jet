.claude/
/target
/deps
/.jet-dev
/notes
*.log

# scripts/install-dev-kernels.sh populates kernels/<name>/ with binaries
# and a kernel.json. Only the kernel.json files are worth tracking.
/test-kernels/*/*
!/test-kernels/*/kernel.json
